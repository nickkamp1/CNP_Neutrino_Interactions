# Lecture 3 — Colab environment.
#
# IMPORTANT: pin these to versions you have actually tested in Colab the week
# of the lecture. Colab's base image drifts and these compiled packages are the
# fragile part. Versions below are placeholders — run `pip freeze` after a
# known-good install and paste the exact versions here.
#
# --- Light stack: expected to install live in Colab (CPU) ---
numpy
scipy
matplotlib
pandas
pyarrow            # parquet I/O for SIREN + Prometheus event output
awkward            # SIREN + Prometheus event arrays
siren              # injection + weights  (a.k.a. the LeptonInjector successor)

# --- Notebook 1 (SIREN): runs FULLY LIVE -- no pre-built cache files ---
# `pip install siren` is a prebuilt wheel (seconds). SIREN then downloads its own
# detector / cross-section / atmospheric-flux data on demand from Zenodo
# (record 20129082) via the bundled `siren-download` CLI. Nothing in data/ is
# needed for notebook 1.

# --- Notebook 2 (PROPOSAL + Prometheus): installed via Prometheus' built-in installer ---
# proposal and prometheus are NOT pip-installable wheels. Notebook 2's Setup cell runs:
#     git clone https://github.com/Harvard-Neutrino/prometheus.git
#     cd prometheus && bash install.sh --with-ppc
# which compiles + installs PROPOSAL (lepton propagation), LeptonInjector (injection),
# and (with --with-ppc) the PPC ice photon path. This takes ~10-20 min in Colab.
# See: https://harvard-neutrino.github.io/prometheus/installation/built-in-installer/
#
# The polished event displays in Part 3 read PRE-GENERATED parquet from
# data/Prometheus_simulation/ and only need pyarrow + awkward (below) -- so they work
# even if the heavy install is skipped. The live PROPOSAL tau-propagation (Part 1) and the
# tiny Prometheus smoke-test (Part 2) need the built-in installer to have run.

# --- Flux: NO separate package needed ---
# SIREN ships atmospheric flux tables (surface AND at-detector, the latter with
# nuSQuIDS oscillation already folded in) on Zenodo record 20129082, fetched live
# by `siren.utilities.load_flux(...)`. The notebook reads them directly -- no
# daemonflux / nuflux / nuSQuIDS install, and no pre-extracted .npz cache.

# --- Discuss-only in the lecture; do NOT try to install live in Colab ---
# GENIE      -> show outputs only
# GEANT4     -> show outputs only
# Tauola     -> tau decays handled inside Prometheus/PROPOSAL for this demo
# pythia8    -> optional hadronization demo, heavy
